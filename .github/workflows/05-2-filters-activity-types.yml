name: filters and activity types part 2
on:
  pull_request:
    types: [closed] 
    branches:
      - main
jobs:
    echo:
        runs-on: ubuntu-latest
        steps:
          - name: echo
            run: echo "Running whenever a PR is closed."