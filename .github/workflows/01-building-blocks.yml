name: "First block"
on:
    push:
    workflow_dispatch:

jobs:
    echo-hello:
        runs-on: ubuntu-latest
        steps: 
            - name: echo hello
              run: echo "hello world"
    echo-goodbye:
        runs-on: ubuntu-latest
        steps:
           - name: goobye
             run: |
                echo I will fail
                echo Goodbye

        